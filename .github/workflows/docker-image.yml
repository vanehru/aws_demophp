name: CI to Docker Hub

on:
  push:
    branches: 
      - master
      - dev
      
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3
        
      - name: Set env BRANCH
        run: echo "BRANCH=$(echo $GITHUB_REF | cut -d'/' -f 3)" >> $GITHUB_ENV
        
      - name: docker build
        run: docker build . --file Dockerfile1 --tag vanehru/demo_php/$env_name:${GITHUB_RUN_NUMBER}
        
      - name: docker push
        run: |
          docker login --username ${{ secrets.DOCKER_USERNAME }} --password ${{ secrets.DOCKER_PASSWORD }}
          docker push vanehru/demo_php/$env_name:${GITHUB_RUN_NUMBER}
